'use strict'

/**
 * IMPLEMENTA: completa la clase en base a la descripción siguiente y al código que aparece en el archivo Funciones.js
 * Esta clase nos ayuda en el procesado de la tabla.
 * Contiene como atributos:
 * - el componente que se está representando en una fila de la tabla
 * - el elemento fila que lo representa dentro de la tabla
 * - si en ese momento está desplegado en la tabla o no
 * - una referencia a la siguiente representación de la tabla (el siguiente componente representado en la tabla)
 */
class RepresentacionComponente {

    componente = null;
    elem_after = null;
    desplegado = null;
    siguiente = null; 
    
    
}